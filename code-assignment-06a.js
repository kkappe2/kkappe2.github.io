let university = 'LSU';

function describeUniversity (myUniversity) {
  let description = myUniversity + ' has a beautiful campus.';

  return description;
};

let universityDescription = describeUniversity(university);
console.log(universityDescription);
